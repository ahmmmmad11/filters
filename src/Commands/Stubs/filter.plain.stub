<?php

namespace {{ namespace }};

use {{ rootNamespace }}Http\Filters\Filter;
use Spatie\QueryBuilder\QueryBuilder;
use {{ namespacedModel }};
use Ahmmmmad11\Filters\Filter;

class {{ class }} extends Filter
{
    public function filter(): Filter
    {
        $this->data = QueryBuilder::for({{ model }})
            ->allowedFilters(
                {{ fields }}
            );

        return $this;
    }
}
